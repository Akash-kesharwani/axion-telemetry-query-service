apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: telemetry-ing
spec:
  ingressClassName: azure-application-gateway
  rules:
  - host: telemetry.cloudpunk.shop
    http:
      paths:
      - backend:
          service:
            name: telemetry-ser
            port:
              number: 80
        path: "/"
        pathType: Prefix

      